

import 'package:flutter/material.dart';

class NaoPronto extends StatelessWidget {
  const NaoPronto({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
